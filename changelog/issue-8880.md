audience: users
level: major
reference: issue 8880
---
The github service `builds` API endpoint now returns builds newest-first instead of oldest-first, a breaking change for consumers relying on the previous order
